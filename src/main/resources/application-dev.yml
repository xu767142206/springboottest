server:
  port: 8081

spring:
  datasource:
    username: root
    password: 325bc52ac86766bd
    #url中database为对应的数据库名称
    url: jdbc:mysql://192.168.1.222:3306/spring?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapper-locations: classpath:mapping/*.xml
  type-aliases-package: com.example.demo.entity

#showSql
logging:
  level:
    com.example.demo.mapper: debug
